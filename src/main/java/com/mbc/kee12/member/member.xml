<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    

<mapper namespace="com.mbc.kee12.member.MemberService">
<insert id="meminsert">
insert into member0402 values(#{param1},#{param2},#{param3})
</insert>
<select id="id2count" resultType="int" >
select count(*) from member0402 where mid=#{param1}
</select>
</mapper> 
